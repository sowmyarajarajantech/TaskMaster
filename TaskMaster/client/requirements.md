## Packages
date-fns | Formatting dates for tasks
framer-motion | Smooth animations for list items and transitions
clsx | Utility for conditional class names (usually part of standard stack but good to be explicit if using heavy dynamic classes)
tailwind-merge | Merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Outfit'", "sans-serif"],
  body: ["'DM Sans'", "sans-serif"],
}
